{
  "name": "cloudflare-uptime",
  "version": "1.6.6",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:init": "wrangler d1 execute uptime-monitor --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute uptime-monitor --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.13.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260717.1",
    "typescript": "^5.5.2",
    "wrangler": "^4.118.0"
  },
  "overrides": {
    "undici": "^7.29.0"
  }
}
